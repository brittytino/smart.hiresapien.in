import PriTestBuilder from '@/components/admin/pri-test-builder';

export const metadata = {
  title: 'PRI Test Builder — Grad360MBA',
};

export default function PriTestBuilderPage() {
  return <PriTestBuilder />;
}
