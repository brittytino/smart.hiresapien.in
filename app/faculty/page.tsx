import FacultyDashboard from '@/components/faculty/dashboard';

export const metadata = {
  title: 'Faculty Dashboard — Grad360MBA',
};

export default function FacultyPage() {
  return <FacultyDashboard />;
}
